## 处理文件上传
- 为了提供性能，会分片并发上传（切片大小可以优化，比如根据网络情况分不同切片大小）
- 为了以后能实现秒传的功能，所以需要对文件进行唯一的标识，所以需要根据文件内容生成唯一hash



## 优化点
- 并发量队列控制，在同一个时间最大并行上传5个分片
- 切片数量的动态调整，自适应
- 服务端合并文件之后校验一下文件，保证文件未被篡改


## 文件流与formdata的区别
用文件流的话只能上传二进制文件
如果用formdata可以同时上传文件和普通字符串